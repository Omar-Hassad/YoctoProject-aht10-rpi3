# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-omar-bsp"
BBFILE_PATTERN_meta-omar-bsp = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-omar-bsp = "6"

LAYERDEPENDS_meta-omar-bsp = "core"
LAYERSERIES_COMPAT_meta-omar-bsp = "scarthgap"
