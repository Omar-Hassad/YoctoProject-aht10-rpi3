# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-omar-distro"
BBFILE_PATTERN_meta-omar-distro = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-omar-distro = "6"

LAYERDEPENDS_meta-omar-distro = "core"
LAYERSERIES_COMPAT_meta-omar-distro = "scarthgap"
